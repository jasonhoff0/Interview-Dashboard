# Office Manager Daily Tracker

Runs every weekday morning to pull the previous business day's GoHighLevel
conversations, compute the average first-response time during business
hours, and write it into the "Daily Tracker" tab of the Daily Metrics
Tracker Google Sheet.

See `daily_update.py` and `.github/workflows/daily_update.yml`.
